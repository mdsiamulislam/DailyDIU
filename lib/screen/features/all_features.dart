import 'package:flutter/cupertino.dart';

class AllFeatures extends StatelessWidget {
  static const String id = 'all_features';
  const AllFeatures({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
